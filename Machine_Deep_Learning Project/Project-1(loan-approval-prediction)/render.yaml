services:
  - type: web
    name: loan-approval-prediction
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --pythonpath app app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
